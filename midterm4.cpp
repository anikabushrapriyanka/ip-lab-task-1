#include<iostream>
#include<conio.h>
using namespace std;
int main()
{
    int num1=80, num2=10;
    if(num1%num2==0)
    {
        cout<<"first is the multiple of the second: ";
    } else {
        cout<<"first is not the multiple of the second: ";
    }
}
